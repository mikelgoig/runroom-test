name: CI

on: [push]

jobs:
  test-php:
    runs-on: ubuntu-latest

    steps:
      - uses: actions/checkout@v2

      - name: Install PHP dependencies
        run: composer install

      - name: Run PHPUnit
        run: composer test
